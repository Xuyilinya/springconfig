# 数据源配置
name: welcome to my party!update
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    data-password: root
    data-username: root
    url: jdbc:mysql://localhost:3306/mp?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
